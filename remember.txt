History of Changes - Gemini Integration Fixes

1. INVESTIGATING 404 NOT_FOUND ERROR
- What: Investigated `test_gemini.py` failing with a 404 error on `gemini-1.5-flash`.
- Why: The provided API key/account did not have access to or no longer supported that specific model.
- How: Listed available models via Python SDK and updated `test_gemini.py` and `chat_engine.py` to use `gemini-2.0-flash`.

2. INVESTIGATING 429 BURST ERRORS (RATE LIMITING)
- What: Discovered 40+ `429 TooManyRequests` errors appearing on the Google AI Studio dashboard simply from running a test script.
- Why: Google's Free Tier strictly enforces 15 requests-per-minute. The `langchain-google-genai` wrapper had an aggressive automatic retry mechanism; when the script hit the limit, Langchain rapidly retried dozens of times in the background.
- How: Added `max_retries=1` to the `ChatGoogleGenerativeAI` constructor in `test_gemini.py` to fail fast, and `max_retries=2` in `chat_engine.py` to prevent hanging. Also added explicit 429 quota exception handling in `main.py`.

3. BYPASSING THE LOCKED QUOTA
- What: The `gemini-2.0-flash` 15-RPM quota bucket was continuously maxed out (locked) by background processes or past loops.
- Why: We needed a way to test and use the API immediately without being rate-limited.
- How: Discovered through testing that newer models have separate quota buckets, so updated `test_gemini.py` and `chat_engine.py` to use `gemini-2.5-flash` instead.

4. MIGRATING TO GEMINI-3-FLASH-PREVIEW
- What: The user reviewed documentation showing `gemini-3-flash-preview` and wanted to try it.
- Why: To use the newest experimental model and enjoy a completely fresh quota bucket.
- How: 
   - Replaced `gemini-2.5-flash` with `gemini-3-flash-preview` in both scripts.
   - Discovered the new 'gemini-3' model returns its response as a list of dictionaries (parts) instead of a plain string.
   - Updated the print statement in `test_gemini.py` to cleanly extract actual text (`"".join(part.get("text", "")...)`) so it displays properly in the terminal.

5. UPDATING PROJECT DEPENDENCIES
- What: Installed the new offical raw SDK `google-genai`.
- Why: The user requested to install it (`pip install -q -U google-genai`).
- How: Ran the install command and permanently appended `google-genai` to `requirements.txt`.

6. PROFESSIONAL GITHUB DIRECTORY REFACTORING
- What: Restructured the entire flat project repository into standardized `app/` and `tests/` directories.
- Why: The user requested a professional codebase structure suitable for uploading to GitHub, keeping core logic separated from testing scripts.
- How: 
   - Created the `app/` python package (with `__init__.py`) and moved `chat_engine.py`, `chunker.py`, `git_service.py`, `graph_builder.py`, `parser.py`, `prompts.py`, and `vector_store.py` into it.
   - Created the `tests/` python package and moved all `test_*.py` and `models.txt` files into it.
   - Updated `main.py` and internal application logic files to use absolute package imports (`from app.module_name import ...`).
   - Created a `.gitignore` to protect `.env`, `temp_repos/`, `venv/`, and `__pycache__` from being accidentally committed.

7. DEPLOYING TO GITHUB
- What: Generated a professional `README.md` and uploaded the entire repository to GitHub (`https://github.com/Roza212/opensource_compass.git`).
- Why: The user requested a professional GitHub layout and automated push so the codebase can be showcased publicly.
- How: 
   - Authored an extensive `README.md` documenting the RAG architecture, FastAPI endpoints, Gemini 3 Flash Preview integration, and pgvector Supabase schema.
   - Linked the remote GitHub branch and executed `git push -u origin main`.

8. END-TO-END SYSTEM TESTING
- What: Activated the FastAPI `uvicorn` development server and executed `tests/test_chat.py`.
- Why: To verify that the fully refactored structural codebase, new Google Gemini API keys, and LangChain endpoints all interact together seamlessly.
- How: 
   - `uvicorn` successfully served the endpoints on port 8000.
   - The test script invoked `http://127.0.0.1:8000/chat` successfully.
   - The LangChain RAG pipeline processed the request and Gemini 3 Flash Preview generated a properly formatted JSON answer without any Rate Limit or ModuleNotFoundError crashes!

9. ARCHITECTURE OPTIMIZATION: FASTAPI STARTUP
- What: Implemented a Lazy-Loading strategy for the PyTorch `SentenceTransformer` model in `vector_store.py`.
- Why: The FastAPI `uvicorn` development server was taking 3-10+ seconds to boot and reload because the global declaration forced the 80MB embedding model to load into RAM synchronously at startup, significantly slowing down the codebase development lifecycle.
- How: Converted the strict `model` initialization into a `_model` global variable tracked by a `get_model()` function. Now, the model statically waits until a user specifically requests the `/embed` or `/search` endpoints before touching the RAM.

10. PHASE 5: MERMAID GRAPH VISUALIZATION
- What: Created `app/diagram_generator.py` with a `generate_mermaid_chart` mapping function.
- Why: The user initiated Phase 5 of the OpenSource Compass project, intending to visually map the repository ast architecture using the Mermaid JS charting UI.
- How: The function correctly traps internal `NetworkX` json data, iterates through all `['links']`, sanitizes internal file paths into valid underscores via `regex`, and constructs an exhaustive `graph TD;\n` string block populated entirely with format `{safe_source}["{source}"] --> {safe_target}["{target}"]`.

11. FASTAPI RESILIENCE & /DIAGRAM INTEGRATION
- What: Finalized `main.py` by integrating the `<generate_mermaid_chart>` script and retrofitting every active router path (`/ingest`, `/embed`, `/graph`, etc.) with universal comprehensive exception handling (`try-except HTTP 500 Server Error`).
- Why: 1. To expose Phase 5 capabilities to the React frontend. 2. To ensure that parsing failures, missing repos, or API outages correctly bubble upwards as standard API JSON error flags instead of unexpectedly terminating the global Uvicorn process.
- How: Heavily refactored `main.py`, preserving functional core logic but injecting strict `try/except` guardrails around the Pydantic routes, returning raw `str(e)` errors safely back to the Swagger dashboard.

12. INSTALLING WATCHFILES FOR RELOAD EXCLUSION
- What: Installed the `watchfiles` python package and appended it to dependencies.
- Why: Uvicorn `--reload` was constantly crashing/reloading the server during `/ingest` because it detects thousands of new files being dumped into `temp_repos`. When we tried to use `--reload-exclude "temp_repos"`, Uvicorn returned a warning stating the flag only works if `watchfiles` is installed securely.
- How: Added `watchfiles` to `requirements.txt` and generated the `pip install` command.

13. MONOREPO RESTRUCTURE & REACT FRONTEND
- What: Split the project into `backend/` and `frontend/` directories. Created a full React + Tailwind dark-mode UI.
- Why: The user requested a professional monorepo structure before open-source showcase. The frontend provides the visual face for OpenSource Compass.
- Components Built:
  - `LandingView.jsx`: Glowing neon input + gradient Analyze button with loading spinner
  - `DashboardView.jsx`: 35/65 split-screen with AI chat panel (user/AI bubbles, auto-scroll, typing indicator) and architecture canvas (fetches Mermaid diagram)
  - `App.jsx`: State controller toggling between Landing and Dashboard views
  - `index.css`: Full custom dark-mode design system with CSS variables, animations, and responsive breakpoints
- Backend Changes: Added CORS middleware to `main.py` so React on port 5173 can reach FastAPI on port 8000. Added `title` and `version` to the FastAPI app instance.

14. BUG FIX: CHAT RETURNING WRONG REPO DATA
- What: The `/chat` and `/search` endpoints were returning results from ALL repositories instead of just the one being analyzed.
- Why: `search_code()` in `vector_store.py` performed a global pgvector similarity search without filtering by `repo_name`. When multiple repos were embedded, the AI would answer with context from the wrong codebase.
- How: Added `repo_name` parameter to `search_code()`, passed it through from `chat_engine.py`, and implemented a dual strategy: tries Supabase RPC with `filter_repo_name` first, falls back to global search + client-side Python filtering if the RPC function doesn't support the parameter yet.

15. MERMAID.JS VISUAL DIAGRAM RENDERING
- What: Installed the `mermaid` npm package and integrated it into `DashboardView.jsx` so the architecture diagram renders as an interactive SVG flowchart instead of raw text.
- Why: The canvas panel was dumping raw Mermaid syntax text (e.g., `graph TD; main_py --> chat_engine_py;`) instead of rendering a visual diagram.
- How: 
  - `npm install mermaid` in the frontend.
  - Added `mermaid.initialize()` with a custom dark theme (purple nodes, blue edges, dark slate background).
  - Created a `useEffect` hook that calls `mermaid.render(id, mermaidCode)` to generate SVG and injects it into a ref container.
  - Falls back to raw text `<pre>` display if rendering fails.

16. DIAGRAM VISIBILITY IMPROVEMENT
- What: Rewrote `diagram_generator.py` to produce cleaner, more readable Mermaid charts.
- Why: Node labels used full file paths (e.g., `backend/app/chat_engine.py`) making every node massive and the entire diagram compressed into unreadable tiny boxes.
- How:
  - Added `shorten_label()` function that extracts just the filename (e.g., `chat_engine.py`) for display labels while keeping full paths as unique node IDs.
  - Changed layout direction from `graph TD` (top-down) to `graph LR` (left-to-right) for better horizontal use of the canvas.
  - Added edge deduplication to prevent duplicate connections from cluttering the chart.

17. CHAT SEARCH 2-STRATEGY REWRITE
- What: Completely rewrote `vector_store.py` search_code() with a robust 2-strategy approach, and updated `chat_engine.py` to always pass repo context to the LLM.
- Why: The Supabase RPC `match_code_chunks` function doesn't return a `repo_name` column in its results. Previous code tried to filter results by repo_name, but since the field didn't exist in the response, it returned unfiltered global results (wrong repo data) or empty results.
- How:
  - Strategy 1: RPC vector search — checks if results actually contain a `repo_name` field. If they do, filters by it. If they don't, falls through to Strategy 2 instead of returning wrong-repo data.
  - Strategy 2 (fallback): Direct Supabase table query with `.eq("repo_name", repo_name)` — guarantees results from the correct repository.
  - Updated `chat_engine.py` to pass `repo_name` into the LLM prompt template and provide a fallback message when no embeddings exist.

18. FRONTEND-BACKEND FULL WIRING (handleIngest)
- What: Updated `LandingView.jsx` with a `handleIngest` function that chains two API calls before transitioning to the dashboard.
- Why: The original `handleSubmit` only called `/ingest`. The user needed `/embed` to also run automatically so the chat has context when the dashboard loads.
- How: 
  - Step 1: `POST /ingest` — clones the repo. Button shows "Cloning repository..."
  - Step 2: `POST /embed/{repo_name}` — generates vector embeddings. Button shows "Generating embeddings..."
  - On success: calls `onAnalyze(repoName)` to transition to the Dashboard view.
  - Dynamic `statusMsg` state updates the button text at each phase so the user always knows what's happening.


19. UVICORN RELOAD CRASH FIX
- What: Renamed temp_repos to .temp_repos across the backend.
- Why: Uvicorn\'s watchfiles on Windows ignored the --reload-exclude flag, constantly rebooting the server during the clone process and causing 500 Internal Server Errors.
- How: Hidden directories (starting with a dot) are natively ignored by watchfiles. Updated git_service.py and all routes in main.py to use .temp_repos.

20. SHALLOW CLONE OPTIMIZATION
- What: Added depth=1 to the GitPython clone command.
- Why: Large repositories (like python-mini-projects) took 5+ minutes to clone because it downloaded the entire commit history, causing frontend HTTP requests to time out.
- How: Repo.clone_from(github_url, target_dir, depth=1) massively reduces download times to seconds.

21. MERMAID DIAGRAM RENDER FIXES
- What: Fixed diagram_generator.py to prevent blank canvases or raw-text crashes.
- Why: Repositories with no imports (or non-Python repos) produced empty graphs (graph LR;), crashing the mermaid.js parser. Isolated Python files also failed to show up.
- How: Refactored generator to explicitly declare all nodes before edges. Added a fallback node if no .py files are found, ensuring the UI always renders a valid SVG.

22. UI ERROR SANITIZATION
- What: Added sanitize_error wrapper around all FastAPI exceptions in main.py.
- Why: When Supabase went down or the URL was invalid, Python\'s Supabase SDK threw an exception containing a massive raw Cloudflare HTML page. The frontend rendered this raw HTML, ruining the UI layout.
- How: sanitize_error intercepts 521 Cloudflare and getaddrinfo errors, replacing them with a single clean sentence. It also limits all other unexpected errors to 300 characters.

23. ENTERPRISE ARCHITECTURAL REFACTORING
- What: Dismantled the monolithic main.py and flat directories into a layered enterprise architecture.
- Why: To ensure long-term scalability and decouple routing, logic, and data schemas.
- How: 
   - Backend: Split main.py into API routers (api/ingest.py, api/chat.py). Moved business logic to services/, stateless utilities to utils/, schemas to models/.
   - Frontend: Extracted all raw fetch() calls out of React components into a dedicated api/client.js file. Promoted components into a views/ directory.

24. END-TO-END ARCHITECTURE VERIFICATION
- What: Performed a full system verification after the enterprise refactoring.
- Why: To guarantee no internal imports or data flows were broken during the transition.
- How: Executed pytest on test_chat.py to verify internal routing, and ran Invoke-RestMethod against the FastAPI routes to confirm uvicorn successfully mounted the new /api/ routers without 404s.

25. ABOUT PAGE & ROUTE NAVIGATION
- What: Created a dedicated About page and added conditional routing logic to App.jsx.
- Why: The user requested a page describing the project's purpose and how it works.
- How: 
   - Created AboutView.jsx featuring a descriptive breakdown of the Vision, Ingestion Phase, Vector Embedding, and AI Mentorship.
   - Updated LandingView.jsx with an "About" link in the top-right corner.
   - Refactored App.jsx to handle routing state (landing, about, dashboard) via conditional rendering.
   - Added hover effects and responsive styling to the new About page cards.

26. MULTI-LANGUAGE AST SUPPORT (CHUNKER)
- What: Extended backend/app/utils/chunker.py from Python-only to 9+ languages.
- Why: The repo could only parse .py files, leaving JS, TS, Go, Java, Rust, C, C++, Ruby, Markdown unindexed.
- How:
   - Created a LANGUAGE_MAP dict mapping file extensions to tree-sitter grammar packages.
   - Installed tree-sitter-javascript, typescript, go, java, rust, c, cpp, ruby.
   - Added a fallback line-based chunker (~50 lines per chunk) for unsupported types.
   - Added a detected_language field to each chunk dict.
   - Updated ingest.py to walk and process all supported file extensions.

27. INCREMENTAL INGESTION & SHA CACHING
- What: Added commit SHA tracking so re-submitted repos skip re-embedding if unchanged.
- Why: Every submission re-cloned and re-embedded the entire codebase, wasting time and Supabase write quotas.
- How:
   - Created a repos table in Supabase (repo_name TEXT PRIMARY KEY, last_commit_sha TEXT, ingested_at TIMESTAMP).
   - Fetched repo.head.commit.hexsha after cloning via GitPython.
   - If SHA matches stored value: return early with status "already_indexed".
   - If SHA differs: delete old code_chunks rows then re-embed; status "updated".
   - Added upsert_repo_sha() and delete_repo_chunks() to vector_store.py.
   - Added a "Loaded from cache" banner in LandingView.jsx.

28. CROSS-ENCODER RERANKING (RAG OPTIMIZATION)
- What: Added a reranking step between pgvector retrieval and Gemini generation.
- Why: Top-k cosine similarity results were often only marginally relevant.
- How:
   - Installed sentence-transformers; uses cross-encoder/ms-marco-MiniLM-L-6-v2.
   - In chat_engine.py: retrieve top 15 chunks, score each with CrossEncoder against the query, keep top 5.
   - Lazy-load CrossEncoder as a singleton (like the SentenceTransformer).
   - Added rerank_scores field to the /chat response for debugging.
   - Logs a warning if all scores are below 0.3 (out-of-scope question).

29. AI SOURCE CITATIONS
- What: AI lists its source files at the end of every answer; rendered as clickable badges in the UI.
- Why: Users had no way to verify which files the AI drew from.
- How:
   - Updated prompts.py: format chunks as [Source: {file_name}], instruct model to produce a "Sources:" section.
   - In chat.py: regex-split AI text at "Sources:" and return sources: List[str] in JSON.
   - In DashboardView.jsx: render source badges below each AI message.
   - Clicking a badge highlights the matching Mermaid SVG node (purple border, auto-scrolls).

30. CODEBASE HEALTH ANALYSIS ENDPOINT
- What: Added GET /analyze/{repo_name} returning a full static analysis report.
- Why: No way to understand codebase quality at a glance.
- How:
   - Installed radon for Python cyclomatic complexity; keyword counting for other languages.
   - Created backend/app/api/analyze.py computing LOC, comment ratio, dependency count, health score.
   - Weighted scoring: 40% low complexity + 20% high comments + 40% low dependencies.
   - Registered router in main.py; added fetchHealthReport() to client.js.
   - Added a "Health" tab to DashboardView.jsx canvas (score card, issues alert, color-coded file table).

31. AI ONBOARDING TOUR
- What: After ingestion, chat panel offers a 5-step AI-generated guided tour of the codebase.
- Why: New developers have no starting point on an unfamiliar codebase.
- How:
   - Added POST /tour in chat.py; uses NetworkX degree_centrality() for top 10 files, asks Gemini to produce JSON tour steps.
   - Added TourRequest schema; added generateTour() to client.js.
   - DashboardView.jsx: shows "Start Tour"/"Skip" on first load; step cards with Prev/Next/Finish navigation.
   - Each step auto-highlights and scrolls the matching Mermaid node via handleSourceClick().
   - Fixed ChatPromptTemplate crash by escaping JSON example braces as {{ }} in system prompt.

32. CHAT HISTORY PERSISTENCE
- What: Conversations saved to Supabase and restored on page reload.
- Why: Every refresh wiped the chat history.
- How:
   - Created chat_sessions table in Supabase (id UUID, repo_name TEXT, messages JSONB, timestamps).
   - Updated ChatRequest to include optional session_id.
   - /chat: reads history if session_id exists, appends new messages, upserts row.
   - Added GET /sessions/{repo_name} and GET /sessions/messages/{session_id}.
   - chat_engine.py: accepts history list, injects last 10 messages as Conversation History into prompt.
   - DashboardView.jsx: session_id in localStorage keyed by repo name; pre-populates chat on mount.
   - "New Chat" button clears localStorage and resets state.

33. ASYNC CELERY INGESTION PIPELINE
- What: Refactored /ingest to be non-blocking using Celery + Redis.
- Why: Large repos caused multi-minute HTTP timeouts; progress bar was fake.
- How:
   - Installed celery[redis] and redis.
   - Created backend/app/worker.py with Celery app and run_ingestion task (clone + chunk + embed + SHA upsert).
   - /ingest: inserts a jobs row (status="queued"), calls run_ingestion.delay(), returns job_id immediately.
   - Added GET /jobs/{job_id} endpoint to poll real status from Supabase jobs table.
   - LandingView.jsx: polls every 2s; progress maps queued=8%, running=animated 55-95%, completed=100%.
   - Created docker-compose.yml defining redis, api, worker services.
   - Added REDIS_URL=redis://localhost:6379/0 to backend/.env.

34. REDIS INSTALLED ON WINDOWS (NO DOCKER)
- What: Installed Redis natively as a Windows Service.
- Why: Docker not installed; Celery requires a running Redis broker.
- How:
   - Downloaded Redis-x64-5.0.14.1.msi from tporadowski/redis GitHub releases.
   - Installed silently via msiexec /qn; auto-registers as a service on port 6379.
   - Verified with: & "C:\Program Files\Redis\redis-cli.exe" ping -> PONG.
   - Redis auto-starts on every Windows boot (no manual action needed).

--- CURRENT STACK STARTUP ORDER (after any machine restart) ---
  1. Redis     - auto-runs as Windows Service (no action needed)
  2. Terminal 1 (cd backend): uvicorn main:app --reload --reload-exclude ".temp_repos/*"
  3. Terminal 2 (cd backend): celery -A app.worker.celery_app worker --loglevel=info --pool=solo
  4. Terminal 3 (cd frontend): npm run dev

36. INCREASED RATE LIMITS
- What: Increased the rate limits across all API endpoints.
- Why: The previous limits were too restrictive (e.g., 5/hour for ingest, 60/hour for chat), making the project difficult to use and test.
- How:
   - Updated ingest.py: POST /ingest limit increased to 20/hour.
   - Updated chat.py: POST /chat and POST /search limits increased to 500/hour.
   - Updated diagram.py: GET /graph and GET /diagram limits increased to 200/hour.

37. HIERARCHICAL TREE VISUALIZATION
- What: Added GET /tree endpoint and graph_to_tree transformation logic.
- Why: To support D3.js collapsible tree visualizations, which are more interactive and scalable than Mermaid diagrams for deep file hierarchies.
- How:
   - Updated graph_builder.py: build_repo_graph now returns a NetworkX DiGraph object.
   - Added graph_to_tree(G, repo_name, repo_path):
      - Identifies root nodes (in-degree 0) or creates a virtual root.
      - Recursively builds a children: [] JSON structure.
      - Handles cycles via is_cycle_ref: true to prevent infinite recursion.
      - Caps depth at 8 levels.
      - Includes metadata: loc, language, imports.
      - Collects stats: total_files, max_depth, language counts.
   - Updated diagram.py: Added /tree endpoint with 100/hour rate limit.
   - Updated chat.py: Fixed generate_tour to handle the new DiGraph return type.

38. ENHANCED TREE DIAGRAM SIDE PANEL
- What: Added a slide-in detail panel and bidirectional navigation to the TreeDiagram.
- Why: To provide deep context for individual files and allow users to traverse the codebase via import relationships.
- How:
   - Backend: graph_builder.py now includes "dependents" (files that import the current node) for each tree node.
   - Frontend: TreeDiagram.jsx now manages a "selectedNode" state.
   - UI: When a node is clicked, the canvas shrinks to 65% width to reveal a 35% detail panel.
   - Features:
      - Path Copying: One-click clipboard copy for relative paths.
      - Bi-directional Nav: "Dependencies" and "Depended on by" sections show related files as clickable chips that pan/zoom the tree to that node.
      - Selection Highlighting: Selected nodes get an outer ring and bold labels.
      - AI Integration: "Ask AI" button pre-fills a context-aware prompt in the chat panel.

39. TREE DIAGRAM LAYOUT OPTIMIZATION (HORIZONTAL)
- What: Switched from vertical to horizontal tree orientation and implemented fixed node spacing.
- Why: To resolve "congestion" where labels overlapped in complex file hierarchies. Horizontal layout is superior for long file names.
- How:
   - Orientation: Root starts on left, children extend right (diagonal bezier curves).
   - Spacing: Used d3.nodeSize([45, 280]) to guarantee vertical (45px) and horizontal (280px) separation regardless of tree size.
   - Visibility: Removed container-level overflow scrolling to avoid D3 zoom/pan conflicts. Added a floating control bar with a Recenter tool.

40. RESPONSIVE VISUALIZATION & TAILWIND V4 CONFIG
- What: Properly configured Tailwind CSS v4 Vite plugin and implemented a mobile-first responsive design for the tree.
- Why: Resolved issues where the layout was "too small" or broken (stacked unstyled elements) on smaller screens.
- How:
   - Config: Added @tailwindcss/vite to vite.config.js to fix missing utility styles.
   - Dashboard: Updated index.css media queries to provide more vertical space (600px/65vh) to the canvas on smaller viewports.
   - Tree Component: 
      - The detail panel now uses fixed positioning (full-screen overlay) on mobile to preserve diagram width.
      - Floating controls adaptively switch from vertical to horizontal based on screen size.
      - Main container uses flex-col on mobile and flex-row on desktop.

41. SEMANTIC TREE ENCODING
- What: Implemented color and size encoding based on node metadata (language and LOC).
- Why: To provide instant visual insight into codebase composition and complexity without needing to click every node.
- How:
   - Color: Language-specific palette (Teal for Python, Amber for JS, etc.) with a floating legend.
   - Size: Node radius scaled by LOC (5px for small scripts, up to 11px for large modules).
   - Tooltip: Added hover tooltips for lightweight info retrieval.
   - Link Fade: Hierarchical link styling (thicker/darker at top, thinner/lighter at bottom) to emphasize the entry points.
